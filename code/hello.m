function [r, theta] = hello(m, n)
% hello - Just for test
%
% Syntax: [r, theta] = hello(m, n)
%
% get random (r, theta) from random points (x, y)
    printf('Hello World!')
    x = rand(6, 6); y = rand(6, 6);
    r = sqrt(a .* a + b .* b);
    theta = atan2(b, a);
end